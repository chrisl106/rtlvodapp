<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RTLVodApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">  <!-- Fixed: Added .NoActionBar to hide the purple hat! -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">#FF0000</item>  <!-- Red like rugby jerseys -->
        <item name="colorPrimaryVariant">#D32F2F</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#03DAC5</item>
        <item name="colorSecondaryVariant">#018786</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <!-- Status bar color (the very top). -->
        <item name="android:statusBarColor">@android:color/black</item>  <!-- Black top now, no purple -->
        <!-- Customize more if you want, but this hides the bar. -->
    </style>
</resources>