<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rtlvodapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />  <!-- New: For saving downloaded APK -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />  <!-- New: For reading downloaded APK -->

    <!-- Say we support big screens like TVs and tablets -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />  <!-- Okay without touch (for TV remote) -->
    <uses-feature android:name="android.software.leanback" android:required="false" />  <!-- For TV style, but not forced -->

    <application
        android:allowBackup="true"
        android:label="RTLVod"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RTLVodApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:banner="@drawable/tv_banner">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />  <!-- Start on TV too! -->
            </intent-filter>
        </activity>

        <activity
            android:name=".VideoListActivity"
            android:exported="false" />

        <activity
            android:name=".VideoPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />  <!-- Don't rebuild on flip -->

    </application>
</manifest>